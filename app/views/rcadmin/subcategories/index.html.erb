        <div id="page-heading">
            <ol class="breadcrumb">
				<li><%=link_to "Category",rcadmin_categories_path%></li>
                <li><%=link_to "Sub Category",rcadmin_subcategories_path%></li>
            </ol>

            <h1>Sub Category</h1>
        </div>
		<%if flash[:notice] %>
		<div class="alert alert-dismissable alert-success">
			<strong>Well done!</strong> <%=flash[:notice]%>
			<button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
		</div>
		<%end%>
        <div class="container">
        
            <div class="row">
            
              <div class="col-md-12">
              
                    <div class="panel panel-sky">
                        <div class="panel-heading">
                        
                            <h4>Sub Category</h4>
                            <div class="options"> 
								<div class="pull-right"><%=link_to 'New Sub Category', new_rcadmin_subcategory_path,:class=>"btn btn-info btn-lg"%></div>
                                <!--a href="javascript:;"><i class="fa fa-cog"></i></a>
                                <a href="javascript:;"><i class="fa fa-wrench"></i></a>
                                <a href="javascript:;" class="panel-collapse"><i class="fa fa-chevron-down"></i></a-->
                            </div>
                        </div>
                        
                        <div class="panel-body collapse in">
                            <table cellpadding="0" cellspacing="0" border="0" class="table table-striped table-bordered datatables" id="example">
                                <thead>
                                    <tr>
										
										<th>Name</th>
										<th>Catgory</th>
										<th>Satus</th>
                                        <th>Created At</th>
                                        <th>Updated At</th>
                                        <th>Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                <% if @rcadmin_subcategories.size > 0 %>
									<% @rcadmin_subcategories.each_with_index do |sc,index|%>
										<%if index%2 == 0 %>
											 <tr class="even gradeC">
										<%else%>
											<tr class="odd gradeX">
										<%end%>
											
											<td><%=sc.name%></td>
											<td><%=sc.category.name%></td>
											<td><%if sc.status == 0%>Active<%else%>Inactive<%end%></td>
											<td><%=sc.created_at.strftime(" %dth %b %Y at %I:%M%p") %></td>
											<td><%=sc.updated_at.strftime(" %dth %b %Y at %I:%M%p") %></td>
											<td><%= link_to 'Edit', edit_rcadmin_subcategory_path(sc),:class=>"btn btn-info" %> <%= link_to 'Delete', sc, method: :delete, data: { confirm: 'Are you sure?' },:class=>"btn btn-danger"%></td>
										</tr>
									<%end%>
                                <%end%>

                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>

        </div> <!-- container -->
